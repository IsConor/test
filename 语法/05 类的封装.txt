类的封装：

1.C语言的封装 --- 结构体
	缺陷 将属性和行为分离
2.C++语言的封装 -- 类
	2.1 将属性和行为作为一个整体，来表现生活中的事物
	2.2 将属性和行为加以权限控制
3.访问权限
	3.1 public：	公共权限，类内可以访问，类外可以访问
	3.2 private：	私有权限，类内可以访问，类外不可访问，子类不可访问
	3.3 protected:	保护权限，类内可以访问，类外不可访问，子类可以访问